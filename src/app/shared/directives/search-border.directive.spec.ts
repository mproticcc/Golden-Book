import { SearchBorderDirective } from './search-border.directive';

describe('SearchBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchBorderDirective();
    expect(directive).toBeTruthy();
  });
});
